{
  "author": {
    "email": "netowls-studio@outlook.com",
    "name": "Yucai Wang"
  },
  "bugs": {
    "url": "https://github.com/source-club/get-started/issues"
  },
  "description": "Git code repository template!",
  "devDependencies": {
    "@types/commit-and-tag-version": "^7.1.3",
    "commit-and-tag-version": "^12.7.1",
    "npkill": "0.12.2"
  },
  "homepage": "https://github.com/source-club/get-started#readme",
  "keywords": [
    "get-started",
    "source-club",
    "git"
  ],
  "license": "MIT",
  "name": "@source-club/get-started",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/source-club/get-started.git"
  },
  "scripts": {
    "dotnet:release": "dotnet tool restore && dotnet tool run nbgv prepare-release --project ./",
    "git:push": "git push --tags && git push --all",
    "pnpm:clean": "npkill -d ./",
    "pnpm:restore": "pnpm install --filter ./src/**",
    "pnpm:restore:root": "pnpm install",
    "pnpm:update": "pnpm update --filter ./src/** -i",
    "pnpm:update:root": "pnpm update -i",
    "npx:ferrflow:init": "npx ferrflow init",
    "npx:ferrflow:check": "npx ferrflow check",
    "npx:ferrflow:changelog": "npx ferrflow changelog",
    "npx:ferrflow:release": "npx ferrflow release",
    "npx:ferrflow:debug": "npx ferrflow release --channel debug",
    "npx:ferrflow:build": "npx ferrflow release --channel build",
    "npx:ferrflow:daily": "npx ferrflow release --channel daily",
    "npx:ferrflow:nightly": "npx ferrflow release --channel nightly",
    "npx:ferrflow:weekly": "npx ferrflow release --channel weekly",
    "npx:ferrflow:alpha": "npx ferrflow release --channel alpha",
    "npx:ferrflow:beta": "npx ferrflow release --channel beta",
    "npx:ferrflow:pre-release": "npx ferrflow release --channel pre-release",
    "npx:ferrflow:try:changelog": "npx ferrflow changelog --dry-run",
    "npx:ferrflow:try:release": "npx ferrflow release --dry-run",
    "npx:ferrflow:try:debug": "npx ferrflow release --dry-run --channel debug",
    "npx:ferrflow:try:build": "npx ferrflow release --dry-run --channel build",
    "npx:ferrflow:try:daily": "npx ferrflow release --dry-run --channel daily",
    "npx:ferrflow:try:nightly": "npx ferrflow release --dry-run --channel nightly",
    "npx:ferrflow:try:weekly": "npx ferrflow release --dry-run --channel weekly",
    "npx:ferrflow:try:alpha": "npx ferrflow release --dry-run --channel alpha",
    "npx:ferrflow:try:beta": "npx ferrflow release --dry-run --channel beta",
    "npx:ferrflow:try:pre-release": "npx ferrflow release --dry-run --channel pre-release",
    "workspace:version:major": "pnpm run --filter ./src/** version:major",
    "workspace:version:minor": "pnpm run --filter ./src/** version:minor",
    "workspace:version:patch": "pnpm run --filter ./src/** version:patch",
    "workspace:version:premajor": "pnpm run --filter ./src/** version:premajor",
    "workspace:version:preminor": "pnpm run --filter ./src/** version:preminor",
    "workspace:version:prepatch": "pnpm run --filter ./src/** version:prepatch",
    "workspace:version:prerelease": "pnpm run --filter ./src/** version:prerelease",
    "version:major": "pnpm version major --no-git-tag-version",
    "version:minor": "pnpm version minor --no-git-tag-version",
    "version:patch": "pnpm version patch --no-git-tag-version",
    "version:premajor": "pnpm version premajor --preid Debug --no-git-tag-version",
    "version:preminor": "pnpm version preminor --preid Debug --no-git-tag-version",
    "version:prepatch": "pnpm version prepatch --preid Debug --no-git-tag-version",
    "version:prerelease": "pnpm version prerelease --preid Debug --no-git-tag-version",
    "version:prerelease:tag": "pnpm version prerelease --preid Debug --no-git-tag-version"
  },
  "version": "2.0.0"
}
